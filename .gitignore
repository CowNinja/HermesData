# Node modules
node_modules/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.venv/
venv/
.env

# Hermes internal
.hermes/sessions/
.hermes/cache/
.hermes_history
.restart_last_processed.json
cron/output/
cron/.jobs.lock
cron/.tick.lock
cron/ticker_heartbeat
_success
*.backup-*
state-snapshots/

# Sub-projects with their own repos (MemoryTools/* all have .git)
MemoryTools/

# ComfyUI (huge)
ComfyUI/

# LSP/tooling with bundled deps
lsp/

# Backups (already on K:)
Backups/

# Temp + caches
tmp/
cache/
audio_cache/
bootstrap-cache/
*.tmp
*.log

# Large untracked project dirs (local-only, not for GitHub)
WisdomVault/
analysis/
archive/
archives/
bin/
copilot/
tests/
Revenue/
Digital-Twin/
config/
config.yaml.bak*
config.yaml.corrupt*
auth.json
auth.lock
backend-probes.cjs
channel_directory.json
benchmark_*.py
benchmark_*.json
benchmark_*.md
update_check

# OS
Thumbs.db
.DS_Store

# Don't push these agents/tools with own sub-repos
agent-tools/
