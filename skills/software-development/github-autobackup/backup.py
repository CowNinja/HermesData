#!/usr/bin/env python3 """ GitHub-AutoBackup Non-negotiable core DNA Backs up Vault + Skills to https://github.com/CowNinja/Hermes-Memory-System.git """ import os import shutil import subprocess from datetime import datetime VAULT_PATH = r"D:\PhronesisVault" HERMESDATA_PATH = r"D:\HermesData" REPO_PATH = r"D:\HermesData\MemoryTools\Hermes-Memory-System" DATE = datetime.now().strftime("%Y-%m-%d") def run_cmd(cmd, cwd=None): result = subprocess.run(cmd, cwd=cwd, capture_output=True, text=True, shell=True) return result.stdout + result.stderr def main(): print(f"=== GitHub-AutoBackup started at {datetime.now()} ===") # 1. Backup Vault (exclude .git) vault_backup = os.path.join(REPO_PATH, "Vault-Backup", DATE) os.makedirs(vault_backup, exist_ok=True) print(f"Copying Vault to {vault_backup}") for root, dirs, files in os.walk(VAULT_PATH): if '.git' in root: continue rel = os.path.relpath(root, VAULT_PATH) dest = os.path.join(vault_backup, rel) os.makedirs(dest, exist_ok=True) for file in files: try: shutil.copy2(os.path.join(root, file), os.path.join(dest, file)) except PermissionError: pass # 2. Backup Skills (exclude .git) skills_backup = os.path.join(REPO_PATH, "Skills-Backup", DATE) os.makedirs(skills_backup, exist_ok=True) print(f"Copying Skills to {skills_backup}") for root, dirs, files in os.walk(os.path.join(HERMESDATA_PATH, "skills")): if '.git' in root: continue rel = os.path.relpath(root, os.path.join(HERMESDATA_PATH, "skills")) dest = os.path.join(skills_backup, rel) os.makedirs(dest, exist_ok=True) for file in files: try: shutil.copy2(os.path.join(root, file), os.path.join(dest, file)) except PermissionError: pass # 3. Git operations print("Committing and pushing to GitHub...") os.chdir(REPO_PATH) subprocess.run("git add .", shell=True) commit_msg = f"AutoBackup triggered manually - {DATE} | Vault + Skills backup with Roemmele/Thompson principles" subprocess.run(f'git commit -m "{commit_msg}"', shell=True) push_output = run_cmd("git push origin main") print(push_output) print("=== GitHub-AutoBackup completed ===") if __name__ == "__main__": main()